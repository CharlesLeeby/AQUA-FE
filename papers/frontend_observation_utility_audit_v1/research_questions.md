# Observation utility / risk 机制审计 v1 — 冻结问题

2026-09-08；六个已知结果 development 物理窗口；观察性机制发现。基于 additive 49c02471716e8ac960e35dd9dd44ef6fbb1428c6 与初始化补充 54cc31fa2ef455ac1e13cdb120cf4b4a2ed15731，不合并旧线。

Q1：相对完整 KLT，L-all/C-all 的覆盖、方向、时序与近似几何信息是否不同？
Q2：A02/A08/Bus 的初始化尝试、尺度/重力与弱条件是否解释整体干预差异？
Q3：刚体运动与光度不一致是否存在；是否足以提出 UNDERWATER_RELIABILITY_HYPOTHESIS？

先完整计算预列诊断量，不训练、不搜索阈值、不实现 admission/router。比较单位是窗口，轨迹/帧/残差高度相关；三次回放仅技术重复。原五种冻结对比及失败全部保留。

## 离线量的固定定义

对 B、XFeat、classical 的当前有效观测统计 q、age_so_far、公开连续长度、最终寿命（仅未来诊断）、4×6网格新增、到KLT距离、射线最近夹角。当前与上一输出同ID计算位移；同一固定IMU积分/标定补偿旋转后计算剩余视差，以及相对最近8条共同KLT轨迹运动中位的偏差。IMU使用配置td与body_T_cam0，零偏先验0；不把剩余视差解释为已知平移深度。

shadow信息并列两个固定模型：(1)单位深度归一化针孔瞬时光流2×6 Jacobian；(2)IMU旋转补偿两帧epipolar translation normal n=R b_prev × b_now 的3×3 Gram。都不是完整VIO Fisher，不包含IMU/边缘化先验、真实深度、鲁棒核、时间相关性或初始化接受门。
H_B=Σ q_i J_iᵀJ_i；H_C同理。固定ridge λ=max(1e-12,trace(H_B)/dim×1e-6)，加入前后同一λ。
全部报告trace、logdet、min-eigenvalue、B最弱方向增益、condition number变化；报告逐候选对B的边际logdet/弱方向、集合logdet、单位观测增益、与B/候选方向共线性。统一q=1的shadow并行计算，不能用其修改冻结后端。全秩B子空间投影残差理论上为0，不能把全秩覆盖误称无新增信息；用最近归一化Jacobian行余弦和主方向重合描述冗余。

原图当前/前帧patch均值与梯度变化、扣除全图亮度变化的局部差、运动一致性仅为可观察代理，不标注颗粒/焦散/折射真值。不得用最终APE生成逐候选标签。

## 最小补充回放条件

旧日志确缺内部scale/gravity/normal matrix和SfM预算，才允许独立日志补丁。先做A02、A08、Bus B/KLT各一对新冻结版/诊断版A/A（6次工程验证，不计27正式回放）。在数据量和时戳一致、无缺失、未对齐绝对位置最大差<=1e-5 m、四元数夹角最大差<=1e-5 rad的严格容差内，才开启该诊断矩阵。容差在任何新回放前固定；不得按结果放宽。任一失败则停止正式诊断，保留失败并标记内部状态不可用于机制结论。A/A只验证B，不证明新增臂对日志开销完全不敏感。

通过后最多A02/A08/Bus×B/L-all/C-all×3=27新正式诊断；无需用满。固定源bag、配置、数学/鲁棒核/权重/初始化/边缘化/solver预算不变，独立端口12681，CPU2,3,8,9/threads1；只停止本任务进程。新诊断不能替代旧结果或消除Bus既有异常。只记录现成状态/矩阵，矩阵谱在离线计算，避免在线新增分解改变时间预算。所有结果/失败入账。

## 决策门

只选择MECHANISM_FOUND / PARTIAL_MECHANISM / NO_DISCRIMINATIVE_SIGNAL / INSUFFICIENT_EVIDENCE之一；A–F解释可多因素但明确主边界。建议算法开发需同时满足用户五项最低证据：三个关键窗一致、准入前可得、包含估计器条件效用/风险或水下有效性、解释至少一来源差异、非明显q混杂。否则唯一下一步为具体缺失机制证据，不自动执行新算法实验。
