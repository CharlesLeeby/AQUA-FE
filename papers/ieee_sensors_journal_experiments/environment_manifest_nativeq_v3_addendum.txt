IEEE Sensors Journal native-q v3 environment addendum
======================================================

Manifest schema: isj-environment-nativeq-v3-addendum-v1
Status: FROZEN_SUPPLEMENT_FOR_CANDIDATE_V3
Date: 2026-08-04
Base manifest: papers/ieee_sensors_journal_experiments/environment_manifest.txt
Candidate: papers/ieee_sensors_journal_experiments/method_lock_nativeq_legacy_candidate_v3.json
Candidate scientific hash: 19b0e4754341d52d6c52e8b3da7409bf22d228ca4a775ae6efe92c072ce71f63

Scope
-----
The base environment manifest is an immutable P03 candidate-v1 snapshot. Its
`every_n=1` and constant-backend-`q=1` lines do not describe the later native-q
legacy candidate v3. This addendum records the candidate-v3 differences without
editing or relabeling the older frozen file.

Candidate-v3 controlled settings
--------------------------------
Preprocessing: adaptive_clahe
Maximum external-feature budget: 350
Backend quality mode: vins_safe
Backend quality floor: 0.8
Backend quality alpha: 0.65
Global constant-q main matrix: false
B0 quality: native VINS behavior
Replay: serial, VINS multiple_thread=0, three technical replays
Replay reducer: median of at least 2 of 3 evaluable replays; hard failure and
solver-risk use any-of-3 aggregation
Scientific unit: sequence, not replay

Candidate-v3 confirmatory sampling
----------------------------------
AQUALOC archaeology: every_n=2, frame_offset=1
AQUALOC harbor: every_n=2, frame_offset=1
NTNU: every_n=2, frame_offset=1
AFRL: every_n=2, frame_offset=0

P06 screening exception
-----------------------
Outcome-blind P06 window screening intentionally evaluates frozen B1 KLT/image
quality at every_n=1 and frame_offset=0. This higher-rate screening identity is
only for deterministic 45 s window scoring; it is not the confirmatory arm
sampling contract and does not change candidate v3.

Known non-scientific display mismatch
-------------------------------------
The frozen candidate-v3 entrypoint
`scripts/run_isj_nativeq_legacy_candidate.sh` prints
`method_profile=isj-nativeq-xfeat-seedchain-legacy-v2` in dry-run mode. The
candidate lock, environment variables, downstream profile, artifacts, and
scientific hash identify v3. The printed line is a display-only provenance bug.
The frozen script must not be edited in place; a final method lock must either
wrap it with a versioned v3 identity shim or register a new candidate version.

Unresolved G3 arm decision
--------------------------
Candidate v3 defines B0, B1, P_legacy, D_legacy, C_legacy, and M, but does not
define the July-plan B2 ungated learned-seed ablation. Therefore candidate v3
is sufficient for outcome-blind P06 screening but is not sufficient by itself
to generate the final arm order. Before final `method_lock.json`, G3 must do
one of the following under a new protocol version:

1. restore a runnable native-q B2 arm and retain the seven-arm legacy matrix;
2. explicitly retire B2 and narrow the admission-specific claim and matrix.

Until that decision and the P04 online C_legacy export-only audit are closed,
the final method lock and G3 PASS must fail closed.
