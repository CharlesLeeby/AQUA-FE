# A08 既有基线重放身份与最早分叉审计

2026-09-10；READ_ONLY_RETROSPECTIVE_ENGINEERING。A08 [2700,3600) 为已知异常开发窗，
不是结果盲预注册，不估计自然失败率，不修改任何原协议/轨迹/门。

## 固定范围

只读以下三个证据包内该窗的全部 B/KLT 记录，不选最好或最坏重复：

- additive 49c02471716e8ac960e35dd9dd44ef6fbb1428c6：B repeat1/2/3；
- observation utility 90646d6a12f0cdde257ff5b7efa3166daa32dc12：aa/frozen B repeat1，
  aa/diagnostic B repeat1（不同二进制，只作单列工程参照，原A/A失败结论不解除）；
- source-neutral quality 6ddca8e5f3a0fb2a64f50bf6e5427d8b7c59986f：B repeat1/2/3。

共8条既有记录，7条标称同冻结后端。锚点固定为最早 additive/B/repeat1，
不是按准确率选择；同时给7条同版轨迹全部21对的直接差异。
原始数据根为 Data offload 的各同名实验目录，仅在本任务目录写派生结果。
不纳入其他窗口、学习臂、原主线不同二进制或历史七月轨迹。

## 三个问题及证据标准

1. 输入bag、节点/库/依赖、相机、除运行路径之外的YAML、播放顺序/参数是否一致？
   验证receipt及现存锁定产物。区分“锁中声明”“现存文件核对”和“当时进程实际加载”。
   端口/输出目录差异列出；未记录的进程环境、host负载、IMU实际接收序列记Unknown。
2. 实际feature接收(时刻/ID/次数/顺序)、首次输出、初始化日志、后续solver事件何时分叉？
   只比较现有字段；日志未记的内部scale/SfM/偏置不推断为相同。
   solver迭代条目数不等于成功步数，termination整数不等于精确终止原因。
3. 耗时/迭代差异与轨迹分叉的先后顺序是什么？
   最早差异不证明其独立因果充分性。没有受控负载/固定迭代对照，不宣称时间预算已确诊。

## 输出与停止

新回放/前端/方法/窗口均0，不构建新后端，不修改solver预算/线程/输入或相机。
不计算新APE/RPE、Sim(3)、对齐或胜负；直接未对齐位置/姿态差属于工程诊断。
原A/A的1e-5m/rad仅用于标记旧容差何时被越过，不改变精度门或重新裁决历史。
CSV纳秒时间保持字符串；float9位solver时间与整数输出头的舍入差明确处理，
不得用近邻误配或相同序号冒充相同时间。
输出一个报告、紧凑身份/分叉表、必要分析脚本和测试、产物清单与决策；
没有日志证据则保留Unknown，不为填表自动启动旧停止的27次矩阵。
本阶段完成并发布后再决定是否需要单独冻结的有限工程对照。
