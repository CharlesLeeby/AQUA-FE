# 当前任务：离线人工点选与保存预测评价

用户授权于 2026-09-11；固定证据 26bc69edffa03da3bfb82f7408c4d384fa2d55ba，沿用 exp/searaft-real-correspondence-v1-20260911。

- 只处理现有 12 对、92 项；按 manifest 顺序，保留空网格和原模板。禁止加载模型、重新推理、换图、改 NCC/FB/2 px 门槛或运行 VINS。
- 交付双击可用、无依赖的 annotate.html：嵌入原 PNG 和固定查询；源/目标自主缩放平移，原图坐标点选；可见/不可见/无法判断，原像素不确定性；标注者一次输入，逐项人工明确确认；撤销、保存、CSV 导入导出。页面不得读取任何算法预测或接受信息。
- 未处理保留 PENDING，AMBIGUOUS 不作错误；不能仅因没找到而标不可见。用户已看汇总接受数，记录这一条件，不称严格双盲。不由模型代填人工确认。关闭前显式导出，不只依赖浏览器缓存。
- 评价入口只读原预测与用户 CSV；先保存人工参考快照，再读预测。逐点误差 e 及 [max(0,e-u),e+u]，u 不是置信区间；2 px 判定与 C/S 精度差异均保留不确定性。完整 92 项、序列、间隔及人工状态分母均报告；部分标注标 PARTIAL，不给完整实验结论。
- 只做坐标往返及 CSV 恢复等必要检查；测试标注留在临时目录。无真实人工标注时，交付工具、说明和一条评价命令后结束，不轮询。
- 正常提交、推送本任务小文件并读回；保留旧报告、模板、输入及 REFERENCE_PENDING。更新独立交接与必要工作日志，不扩建平台。
