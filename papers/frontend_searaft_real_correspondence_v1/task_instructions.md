继续 AQUA-FE。本条为完整任务，不依赖其他聊天。

用户优先目标：
尽快找到可实际利用的学习测量优势。
不为维持工作而重复审计、调阈值或扩建平台。

本轮任务：
SEA-RAFT REAL CORRESPONDENCE CHECK

中文：
SEA-RAFT真实图像对应点小型验证。

==================================================
1. 唯一问题与任务边界
==================================================

只回答：

“在真实图像中，固定的SEA-RAFT＋当前图像证据检查，
是否能够找到强LK重试找不准的同一个物理位置？”

已有证据：
SEA-RAFT预测与模型锁：
a2f3bedcc2e4f98bd18f99a3601d70433de42aa7

固定图像证据检查：
4d061b8b46148e323806478acc28527b37e5b09b

旧CONTROLLED_GAIN_ONLY和EVIDENCE_GATE_NOT_SUPPORTED
保持原样，不修改标签、门槛或旧结论。

本轮另立真实测量问题，不重跑原48对合成测试。

授权：
- 复用已有模型、代码、图像和有效缓存；
- 运行下述固定12对真实图像；
- 生成少量对应点核对材料；
- 正常commit/push本任务代码与小型结果。

不做：
训练、微调、换权重、调NCC、增加新增强、
VINS回放、初始化审计、大规模窗口扫描。

先把本条保存为task_instructions.md，
再用一页protocol.md固定本轮设置。

==================================================
2. 沿用现有环境，固定模型和检查
==================================================

项目：
https://github.com/CharlesLeeby/AQUA-FE

优先复用现有SEA-RAFT隔离环境及本地模型。
新建任务分支和结果目录，不覆盖旧实验：

papers/frontend_searaft_real_correspondence_v1/

模型及推理设置完整沿用a2f3bed的model_lock：
同一spring-M权重、4次迭代、原缩放和FP32设置。

固定证据检查完整沿用4d061b8：
11×11原灰度亚像素patch、
NCC≥0.65、原有限值/方差/完整采样域规则。

不得：
- 把阈值改成更容易通过的数值；
- 改预测坐标或再用LK精化SEA-RAFT输出；
- 改遮挡标签；
- 为不同图像选择不同配置；
- 重新下载已有且身份正确的模型。

只读取上述报告、锁文件、输入清单和必要实现。
不全文重审仓库。

==================================================
3. 预先固定12对真实图像，不看输赢选样
==================================================

沿用原三个200 raw帧片段：
A02 [0,200)
A08 [2700,2900)
H02 [0,200)

精确bag、时间戳和索引映射读取旧输入清单。
prepared bag内索引不能再次套用原序列偏移。

每个片段固定两个源帧：
片段内offset 40和120。

每个源帧分别与：
t+1
t+10
配对。

总计：
3片段 × 2源帧 × 2间隔 = 12对。

t+1代表原相邻帧条件；
t+10代表人为降低取样频率后的较长间隔条件。
必须报告实际时间差，不能假设统一帧率，
也不能把t+10收益包装成原相邻帧的自然收益。

直接使用真实帧：
不施加人工平移、光照变化、模糊或遮挡。

若某对确实缺失，记录缺失，不按结果换另一对。
这些图像属于开发数据，不称held-out。

==================================================
4. 固定少量查询点，避免只看成功案例
==================================================

每个源帧最多8个查询点：

- 从该源帧已有GFTT/KLT点中选择；
- 使用4×2网格，每格选择距离格心最近的一个点；
- 点距图像边缘至少16原像素；
- 相同源帧对t+1和t+10使用同一批点；
- 空格保留为空，不换图或扩大搜索挑点。

最多96个对应查询。

选择只依赖源图和源点，不读取C/S预测、
目标误差或后续存活情况。

不能只选C失败、S成功的点作为整个测试分母。

先保存pair_query_manifest.csv，再计算新预测。

==================================================
5. 只比较现有两种测量方案
==================================================

C0：
强LK重试，沿用已有31窗口、4层、40迭代，
以及原共同FB/有限值/边界口径。

S0：
SEA-RAFT直接位移，沿用原共同FB/有限值/边界口径。

C1：
C0加4d061b8的固定patch证据检查。

S1：
S0加完全相同的固定patch证据检查。

主要比较C1与S1。
C0/S0只是同一批预测的诊断，不另开方法搜索。

每对正反向SEA-RAFT各计算一次，所有点共享。
身份完全相同的已有缓存可以复用，
不得逐点重复运行整图网络。

记录预测坐标、有效性、FB、NCC和耗时。
不建设轨迹管理，不生成VINS feature bag。

==================================================
6. 真实参考对应：不能用算法自证正确
==================================================

首先检查是否已有适用于这些像素的独立人工或标靶对应标注。
没有就准备盲态对应核对材料。

禁止将以下内容当真实参考：
- SEA-RAFT预测；
- C/LK预测；
- 后续普通LK三步通过；
- 光流循环一致；
- VINS/COLMAP位姿投影；
- 从同一批候选计算出的几何拟合结果。

标注材料：
源图显示固定查询点及ID；
目标图不显示任何C/S预测；
目标图不能以某个算法预测为中心裁剪。

可以显示原图和放大局部，但不能通过预测泄露目标位置。
不建设Web标注平台，普通图片和CSV即可。

参考状态至少区分：
VISIBLE_CORRESPONDENCE
OCCLUDED_OR_OUT_OF_VIEW
AMBIGUOUS

存在可确认对应时记录目标坐标和标注者身份。
记录可辨认精度或标注不确定性；
模糊纹理不能伪造亚像素真值。

如果只有模型视觉判断，没有真实人工核验：
准确称为“待核对标注提议”，不称独立人工GT。
同一模型看两次，不算两个独立标注者。

没有已确认标注时也不要只停在“需要真值”：
仍完成固定12对推理、完整预测表和待核对图。
最终明确列出需要用户核对的具体文件与点，
不能泛泛要求重新上传整个数据集。

不伪造人工核验，不根据预测结果修订参考答案。

==================================================
7. 有参考就评价，没参考就明确待核对
==================================================

对已确认可见对应报告：
- 原始预测端点误差；
- 中位数、p95；
- ≤2原像素比例；
- 有效接受覆盖；
- C1/S1各自正确与错误接受；
- S1独有正确对应；
- C1独有正确对应。

标注不确定性接近2px时：
给出不确定状态或误差范围，
不能强行按2px切成对错。

不可见和模糊点：
保留在完整清单中；
分别报告算法是否接受，
不要从分母悄悄删除。

有人工确认的不可见标签才评价不可见误收。
AMBIGUOUS不自动等于不可见或算法错误。

分别汇总：
- 三个片段；
- t+1与t+10；
- 全部查询；
- 参考已确认与待确认数量。

不从最多96个相关查询推断总体成功率。
不把一对图像中的多个点当独立场景。

尤其区分：

1. C/S端点接近，只是接受门不同；
2. S端点确实更接近独立参考；
3. C端点更正确；
4. 参考不足，无法判断。

只有第2类才能直接支持学习位移精度的额外优势。
第1类可以研究有效性判断，但不能称C完全找不到位置。

==================================================
8. 本轮结论直接服务下一步决策
==================================================

不要求所有序列、所有间隔全面胜出。
但必须明确优势发生在哪里、付出什么错误与计算代价。

最终只能选择：

REAL_MEASUREMENT_GAIN_SUPPORTED：
真实参考支持明确的学习测量优势，
差异不能仅由标注不确定性或接受门解释。
限定具体片段和间隔，不宣称泛化。

LOCAL_OR_MIXED_GAIN：
存在局部正确优势，也存在损失或证据范围过小；
如实交付，不自动扩大。

NO_REAL_INCREMENT：
在本批固定可评价对应上没有实际增量；
停止本版本，不换图寻找成功。

REFERENCE_PENDING：
推理和材料已经完成，但缺少足够独立参考；
列出待核对内容，不编造精度胜负。

DATA_OR_RUNTIME_BLOCKED：
具体输入或运行问题，保留已完成数和错误。

本轮不再追加合成门、不磨1%误收阈值、
不训练、不接VINS。

若有真实测量优势，只交付供下一次最小集成使用的固定候选。
本轮成功不自动构成论文创新。

==================================================
9. 最少交付和GitHub
==================================================

只需：
protocol.md
pair_query_manifest.csv
predictions.csv
reference_annotations.csv
comparison.csv
decision.json
report.md

参考未确认时comparison明确标REFERENCE_PENDING，
不能填写看起来完整的虚构指标。

附少量盲态核对图和算法对照图，二者分目录；
不把算法预测提前显示在参考标注材料中。
不建立大型报告平台。

交接入口：
docs/CODEX_HANDOFF_SEARAFT_REAL_CORRESPONDENCE.md

正常commit/push必要代码和小型文件。
不上传bag、权重、大flow缓存，
不force push，不修改其他工作区。

推送后读回report、decision和核心表格，
返回真实分支和commit固定链接。

==================================================
现在执行
==================================================

先固定12对真实帧和查询点。
复用现成模型与检查，完成预测。
用独立参考判断有没有真实对应优势。

没有参考就交付可直接核对的材料，
不要假装得到了真实准确率，也不要只写一句“需要标注”结束。

本轮要回答的唯一问题：

“除了合成变换，SEA-RAFT是否真的把某些真实目标位置
找得比强LK更准？”

答案决定是否值得接入AQUA-FE，
不再用更多规则替代这项证据。