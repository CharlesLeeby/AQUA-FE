继续 AQUA-FE。本条为完整任务。

用户目标：
只做能直接改变下一步决策的小实验。
不自动重训，不换数据，不扩模型，不启动VINS。

当前证据提交：
cd615f039d7e7daa75ab5cc1b217283846d86dc8

当前目录：
papers/frontend_temporal_observation_refinement_tartanair_v1/

本轮只做：
固定出生参考点的离线消融。

一、保留原结论

原NO_TEMPORAL_REFINEMENT_GAIN保持不变。
原P/T训练、checkpoint、预测、评价及停止规则全部冻结。
不覆盖任何旧产物，不把新分析倒填成原实验通过。

MIMIR的SUPERVISION_UNAVAILABLE也保持原样。

本轮是看过原结果之后提出的开发诊断，
不是独立确认实验，不宣称新的泛化结果。

二、问题非常具体

当前监督将每条轨迹出生像素锚定为固定三维参考点，
所以出生观测的B误差按定义为零。

当前网络没有显式强制出生修正为零。

只回答：

“保留出生参考位置不变，能否减少无效修正，
以及非出生观测本身是否已经具有实用精化收益？”

不探索：
年龄阈值、质量门、修正幅度、缩放系数、
新损失、新网络、更多数据或更多训练步数。

三、只读已有预测，禁止新训练与网络推理

直接读取现有训练/验证/测试缓存及已保存B/P/T预测。
优先使用本地原运行目录：

/mnt/data/AQUA-FE_WS/experiments/temporal_refinement_tartanair_v1

准确文件从现有报告和运行清单定位，不猜文件名。
测试逐观测预测已经存在，不重新跑模型。

本轮：
优化器更新0；
网络推理0；
VINS回放0；
下载0。

允许正常的数组计算及现有评价函数。
必要文件确实缺失时，说明具体路径，不重建整个数据流程。

四、出生定义不能依赖误差或有效标签

使用完整B轨迹历史中的：
sequence + track_id + 实际出生frame。

不能将：
“第一个有效标签”
“误差为零”
“第一次进入某个时间块”
当成轨迹出生。

若保存窗口从已有轨迹中途开始，
必须区分真实出生与窗口左截断；
不能为了让修正归零而重新定义出生。

五、固定五组结果

B：原KLT。
P：原逐帧模型输出。
T：原时序模型输出。

P-anchor：
仅实际出生观测delta=0；
其余delta与原P完全相同。

T-anchor：
仅实际出生观测delta=0；
其余delta与原T完全相同。

不修改真值、不重新锚定三维点、不删除出生观测。
不重新选checkpoint，不改变patch有效性。
不根据标签选择哪些非出生点保留修正。

这是固定边界条件消融，
不是利用GT决定每个点是否需要修正。

六、只输出必要的分解

对验证集和测试集分别给出：

1. 全部有效观测：
   EPE median/p90/p95、>1px、>2px。

2. 实际出生观测：
   数量、原P/T修正幅度和由修正引入的误差。

3. 非出生观测：
   相同完整分母上的B/P/T误差；
   不再按测试结果寻找最佳年龄区间。

4. 时序误差变化：
   出生→下一有效连续帧；
   两端均非出生的连续帧；
   完整原时序指标。

5. 原固定100帧块：
   保留全部块，不能隐藏500–600块的退化。

原年龄分组表可引用，不增加新的年龄切分搜索。

同时回答：

- 出生修改解释了多少整体指标变化？
- 去掉出生修改后，跟踪阶段有没有真正超过B？
- T相对P是否仍有独立的时序收益？
- 还是只把按定义零误差的参考点恢复了原状？

若出生置零改善总体指标，
不能直接称为学习跟踪能力提升；
必须看非出生观测和原跨块结果。

七、决定与停止

保留原完整评价判据，并列出新消融是否达到。
不把门从20%改成5%，不新增有利的通过口径。

如果：
仅出生观测变好，
非出生精度/时序指标仍无实用增量或跨块仍退化：

结论：
ANCHOR_FIX_INSUFFICIENT。
结束当前原型，不重训、不启动VINS。

如果：
固定参考后，已有模型在非出生观测上存在明确净收益，
且原总体及跨块条件也得到支持：

结论：
ANCHOR_CONSISTENCY_MATTERS_DEVELOPMENT。
仍然只作为开发线索，
交回主规划窗口决定下一步。
不自动训练第二版，不直接进入真实系统。

如果：
无法从现有记录准确辨认出生或重建相同评价：

结论：
DIAGNOSTIC_UNRESOLVED。
说明缺失，不扩成数据集审计。

不能由这个单一检查推断：
所有学习精化有效/无效；
所有退化都由出生帧造成；
真实水下VINS已经得到改善。

八、交付保持最小

不再创建完整新实验框架。
在现有实验目录下建立独立子目录：
anchor_consistency_check/

只需：
一个可复用分析脚本；
comparison.csv；
一页report.md。

把本条完整任务保存到该子目录，
报告中写明原提交、读取文件和唯一干预。

本次正常commit/push授权仅限相关小型文件。
不修改默认模型、不覆盖旧decision、
不force push、不上传训练缓存和权重。

现有交接追加：
原版本仍NO_TEMPORAL_REFINEMENT_GAIN；
本次为独立的无训练边界条件诊断；
实际结果及唯一决定。

远端只核对更新的报告和比较表，
不重做全库哈希。

现在用已有预测完成这一项固定计算。
不要重新提出研究路线或再训练一个版本。
