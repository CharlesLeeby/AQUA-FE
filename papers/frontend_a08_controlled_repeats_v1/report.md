# A08 原预算工程重复：1/3完成，并发条件阻塞

更新时间：2026-09-12T21:23:00+08:00。
**PARTIAL / WAITING_RESOURCE；1次新回放完成，另外2次未启动。**
没有改前端、后端源码/二进制、输入、数学YAML或任何门。没有算法修复、新正例或扩窗。

## 做了什么

继上轮只读定位，先冻结[协议](preregistration.md)与[执行身份](execution_lock.json)，
再直接调用原49c0247证据对应的runner，运行同一A08 [2700,3600) KLT输入。
仅重定向任务输出/ROS端口，保持原32/40ms、8迭代上限、CPU2/3/8/9、三种数学库线程1、
rate1/delay3/drain8。复用已有bag和容量证明，没有重新生成前端。
新增最小进程外观察器；4项新测试和5项既有时间/配置分析测试通过。
实验脚本见[runner](../../scripts/run_a08_controlled_repeats.py)，
[测试](../../tests/test_a08_controlled_repeats.py)。旧冻结文件、旧结果均未覆盖。

根盘约2.10GiB、运行盘约13.5GiB、内存满足原线，启动时资源预检通过。
本阶段当前阻塞是**另一个工作区的连续VINS回放**，不是旧的磁盘不足。

## 为何没继续启动其余两次

首次r1启动前发现其他VINS，runner返回WAITING_RESOURCE，没有启动本次节点。
该进程结束后再检查通过，r1于21:16:58+08:00开始；但另一个工作区随后启动了新的VINS。
运行中两次只读进程快照确认重叠。r1完整结束并保留；到r2的启动检查时，对方仍在运行，
原runner主动拒绝继续。r2/r3未启动，未结束或修改任何对方进程。

这暴露的是**单次启动检查不能保证整个回放期间没有并发**，不是已修复的数值问题。
不能把r1当成“无其他研究回放”的有效证据，也不能称三重复验证完成。
准确阻塞记录及观察边界见[resource_incident.json](resource_incident.json)。
不会自动重跑r1替换这条受干扰记录；如要补充干净重复，须另行记录对原固定三次预算的授权，
不能偷偷变成“多跑几次挑三次”。剩余两项也需要协调可用时段。

## 已有的1次结果（工程差异，不是APE）

| 记录 | 完成/初始化 | 位姿 | 头时间跨度覆盖 | 原始最大位置范数 |
|---|---|---:|---:|---:|
| 固定历史锚点 additive B r1 | 既有完成 / 1次init标记 | 440 | 97.774% | 3.666900 m |
| 本次 B r1 | COMPLETE / 1次init标记 | 440 | 97.774% | 3.311275 m |
| 本次 B r2 | NOT_STARTED_RESOURCE | — | — | — |
| 本次 B r3 | NOT_STARTED_RESOURCE | — | — | — |

覆盖定义为输出头跨度43.893621248s / 完整feature输入跨度44.892880440s，
不是proxy common-support覆盖。新r1与锚点均接收450帧/157500观测，
记录的接收时刻/ID/次数/顺序摘要一致；完整440个输出头与solver序列对应一致。
没有新增common-support APE/RPE、evo、SE(3)/Sim(3)或胜负分类。

相对固定历史锚点，首次保存位置与迭代条目数差异都在输出第3次：
同一32ms预算，锚点9条/36.204997ms，新r1为8条/32.161207ms。
该时刻记录的eligible/residual ID/计数/顺序相同；首次该集合差异在第40次。
整段最大直接位置差0.355843060m，最大直接姿态差0.006766378rad，越过原严格A/A容差。
这与上轮“求解工作量会不同”的观察一致；不是时间预算或并发负载导致最终差异的独立证明。
没有重现之前约283km原始位置范数异常，但一次未重现绝不等于异常已修复。
[完整结果表](replay_results.csv)、[逐对工程差异](pairwise.csv)、[逐事件证据](events.json)。

## 新补齐的身份与仍然未知的部分

本次r1实际进程的二进制/库、相关已锁依赖、CPU亲和及线程设置检查PASS；
采集13次进程外样本，1min load为2.0–2.8。首次映射快照记录178个实际映射文件，
其摘要在播放后计算，详见[process_identity.json](process_identity.json)。
这是实际启动后的快照，不是只查磁盘上哪个库存在；仍不等于每个时刻的完整动态加载证明。
保留相关环境白名单和摘要，不发布凭据或完整环境。

自动[decision.json](decision.json)的loaded_environment_checks=false表示
**预定3次还未全部完成并核对**，不是r1加载身份检查失败；逐次PASS见结果表。
历史实际进程环境、数值pre-solve状态、每次实际IMU消费顺序、精确Ceres停止消息仍 **Unknown.**
Ceres迭代条目包括初始条目，9条不等于9个成功更新；termination=1不能区分时间/迭代上限。

**Confirmed fact.** 1次新记录完成且有实际环境证据，输出与旧锚点不同；期间有其他VINS并发。
**Hypothesis / Inference.** 有限墙钟预算可能放大环境差异，但本次没有控制负载或隔离因果。
算法收益、消除A02风险并保留A09/Bus收益、learned优于KLT/现代学习方法、新窗口验证均 **Not evaluated.**
原主线仍SAFE_FALLBACK_ONLY/NO_EXPANSION，不能拿本次工程现象抹去A02删点充分性。

## 身份、公开范围与唯一下一步

本次源码基底main@f6f8feec66c2faf1f59cdb67c1e817028a3bccaf加执行锁精确文件SHA；
旧执行器证据49c02471716e8ac960e35dd9dd44ef6fbb1428c6，node e231871e…、lib86c1f977…、
feature bag17305db3…，完整SHA在执行锁。原固定350主线与本容量1000诊断二进制不混表。
原始vio/日志/feature bag路径见结果表及锁；本次不上传bag/完整日志/模型。
[原始产物与派生文件哈希](artifacts.sha256)。报告发布commit另由GitHub读回回执给出。

**唯一下一步：协调其他工作区停止启动回放的时间段，再按冻结顺序继续r2/r3。**
本阶段PARTIAL证据先发布，不无人值守轮询，不修改后端时间预算或偷偷增加重复次数。
未来补齐时新增完成态分析文件/快照，保留本次PARTIAL表和报告，不覆盖它们。
