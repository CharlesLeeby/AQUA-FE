# SEA-RAFT direct recovery v1 — 结果前冻结

基点1138fab635910d3e1c34d57f99dab84c6ff8e9f5；授权见task_instructions.md。唯一差异D删除31/4强LK步骤，原21/3 KLT失败旧点直接SEA-RAFT；成功观测不改、不LK精化。原模型/FP32/iters4/scale−1/raw mono8预处理，原有限值/FB≤1/边界8/11×11有效patch/std>1/NCC≥.65、恢复q/sigma、GFTT350、同帧同ID、已修正ROS浮点时间及速度、每2帧offset1公开全部不改。D自行因果演化，不剪贴旧R。

**固定输入/执行。** H02然后A02，各raw[0,900)，每raw处理、450公开帧；精确输入沿基点input_manifest，A02第901图不处理。先完整B/D导出；B逐字段/序列化匹配原B，普通成功点不改、零强LK调用、连续ID/有限导出通道/350容量、非feature消息一致。已有容量/接收诊断，无新后端编译。正式顺序H02 B1,B2,B3,D1,D2,D3；A02 B1,B2,B3,D1,D2,D3，共最多12新replay。所有B全新，不选历史最好。D/B输入相同则映射B对应重复，不另跑D。保持既有backend二进制/依赖/数学YAML，仅输出路径和隔离ROS端口12741。所有失败与重复保留，不第四次。

**缓存与成本。** 本地官方模型不下载。旧缓存来自同一冻结bag身份、同一相邻raw索引/前后时间戳、同模型和原像素坐标系；仅精确查询坐标命中，不按ID或近邻凑。优先旧系统npz，其次原自然流精确查询。缺失查询按一对一次正反向共享推理，已命中预测原样保留；每相邻对最多新增一次，合计≤1798。新缓存保存所有D查询、图像摘要和前后时间，杜绝序列化修复重推理。单列完全缓存对、含缓存查询对、命中/新增查询、新网络对/实际方向调用、模型加载、CPU前端和总墙钟。不存在模型预热或额外真实图像探针。

**评价合同。** 主比较每窗D/B六轨共同支撑；辅助D/旧R、D/旧C各重新计算六轨共同支撑，不用旧标量拼接，注明跨批次环境/重复波动。fixed-scale proper SE(3) APE RMSE主指标，严格1秒平移RPE护栏。沿基点：共同≥30pose、≥10s、≥70%coverage、≥10严格1s pair，evo差≤1e-6m。APE实用改善≥max(对照中位5%,.01m)，且严格大于两臂最大极差；RPE增量≤max(对照中位5%,.005m,两臂最大RPE极差)。反向APE同门或RPE越界为实用损失；严重增量>max(对照10%,APE .01m/RPE .005m,对应极差)。每个D重复相对本轮B中位超过max(10%,相应绝对下限)单列严重异常，不以中位掩盖。D自身极差超过max(D中位10%,相应绝对下限)标重复不稳定。B普通波动如实进入上述极差门并报告，不单独叫实现失败；真实输入错误、非有限、明显数量级发散/无效支持限制可靠结论且不重跑。

**决策。** 无D/B输入差异→NO_LEARNED_INTERVENTION。正式D/B严重退化、D单次严重异常/明显重复不稳定、无效支持/运行条件阻止可靠结论→UNSAFE_OR_UNRESOLVED。至少一窗D/B实用净收益，另一窗无不可接受严重退化且D无严重异常→DIRECT_RECOVERY_PROMISING；普通B波动本身不自动否定已超过重复极差的增量。无本轮D/B实用收益，但至少一窗D/旧R实用改善且无辅助严重反向变化→REMOVING_CLASSICAL_RETRY_HELPS_BUT_NO_GAIN；其余有介入无实用增量→NO_DIRECT_LEARNED_INCREMENT。仅胜发散C不算成功。辅助旧C异常/跨批次不用于提升正式结论。

所有APE/RPE报告3次min/median/max，技术重复不独立科学样本。恢复事件非独立点；接收/残差按原ID和时间关联，只说明带恢复历史轨迹的使用层，非每个因子都来自一次学习恢复。初始化/GFTT变化未隔离，COLMAP/proxy非独立GT，已知开发窗非held-out，逐点正确性Unknown。旧结论不改。一次后停止；D不支持则结束当前SEA-RAFT当帧恢复组合线，不自动扩窗、调参、换模型或标注。
